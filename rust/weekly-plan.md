# Rust 每周学习计划

本文件由周日 6:00 自动化维护，用来控制“一周能学完”的 Rust 知识点范围。

## 规则

- 每周日生成或更新本周计划。
- 本周计划优先包含上周未完成的 `pending` / `planned`。
- 每周知识点数量控制在 3 到 5 个。
- 知识点按优先级排序：简单、基础、常用优先；用户反复追问的知识点优先。
- 每日 6:00 只从本周计划中抽取一个未掌握知识点。
- 如果存在 `pending`，每日自动化只提醒继续学习该知识点，不推送新的知识点。
- 推送内容必须循序渐进：每次只引入一个新 Rust 学习目标，示例代码中的其他 Rust 语法必须是已学过内容。

## 当前周计划：2026-W25

周期：2026-06-15 至 2026-06-21

本次重置日期：2026-06-14。上周只完成了 `ownership 与 move`，`借用与引用 &T` 已在 2026-06-14 推送并在 2026-06-14 学完，因此本周按借用前置关系继续推进。高级旧条目继续留在 backlog，不提前进入 planned。

| 顺序 | 知识点 | 状态 | 优先级 | 为什么本周学 |
|---:|---|---|---:|---|
| 1 | 借用与引用 `&T` | done | 106 | 上周流转；ownership 的直接下一步；先补清“借用不拿走所有权”和“引用本身可 Copy” |
| 2 | 多个不可变借用可以同时存在 | done | 103 | 继续巩固 `&T`；把“只能读、不能改”和“可同时读多个地方”分成独立目标 |
| 3 | 可变借用 `&mut T` | planned | 101 | 在 `&T` 心智模型稳定后，再进入“独占地改值” |
| 4 | 借用冲突规则：不能同时存在 `&T` 和 `&mut T` | planned | 100 | 帮用户把“像 C 指针那样改值”与 Rust 的借用检查规则对应起来 |
| 5 | `Option` 的 `Some` / `None` | planned | 97 | 借用规则打稳后，才进入第一个常见枚举类型，避免知识点叠加过多 |

## 前置关系说明

- `ownership 与 move` 已完成，所以下一步才能讲 `&T`。
- 只有先理解“不可变借用只是读”，才能继续讲“多个不可变借用同时存在”。
- 只有在 `&T` 规则稳定后，才进入 `&mut T` 的独占修改。
- 只有在 `&T` 和 `&mut T` 都见过之后，讲“读写不能同时发生”的冲突规则才不会跳步。
- `Option` 放在借用基础之后，避免把“引用/借用”和“枚举匹配”混在同一天学习。

## 流转说明

- 上周未完成并且符合前置知识的条目：`借用与引用 &T`，已流转为本周第 1 项。
- 上周未进入学习的基础 backlog：`可变借用 &mut T`、`Option`，本周在前置知识满足后进入 planned。
- 旧高级 backlog：`Result` 与 `?`、`ok_or_else` 和 `?`、trait 里的 `impl Future + Send` 继续保留在 backlog，等 `Option`、错误处理、trait/async 等前置完成后再进入 planned。
- 用户对 `println!`、`{}`、Copy/move、`&str` 是否 Copy、重新赋值是否影响原变量、如何像 C 指针那样修改值问得较多，所以本周说明会优先融入这些薄弱点。

## 周日生成清单时的输出要求

周日推送给用户时，输出：

- 本周要学的知识点列表。
- 每个知识点为什么值得学。
- 建议学习顺序。
- 上周未完成内容的流转说明。
- 提醒用户：每日只会在前一个知识点学完后再推送下一个。
